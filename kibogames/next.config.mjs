/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['kibogames.com', "upload.wikimedia.org", "lh3.googleusercontent.com"],
    },
};

export default nextConfig;
